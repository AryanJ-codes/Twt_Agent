[phases.build]
cmds = [
    "python -m venv /opt/venv",
    ". /opt/venv/bin/activate",
    "pip install -r requirements.txt",
    "playwright install --with-deps chromium"
]

[phases.start]
cmds = [". /opt/venv/bin/activate && python main.py"]
